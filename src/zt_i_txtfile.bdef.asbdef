interface;
use draft;

define behavior for ZT_I_TXTFILE alias txtfile
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}