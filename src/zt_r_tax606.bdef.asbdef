managed implementation in class zbp_t_r_tax606 unique;
strict ( 2 );
with draft;

define behavior for zt_r_tax606 alias tax606
persistent table ztax_606
draft table ztax_606_d
lock master
total etag LastChangedAt
etag master LocalLastChangedAt
authorization master ( instance )
//etag master <field_name>
{
  create ( authorization : global );
  update;
  delete;
  field ( readonly, numbering : managed ) Uuid;
  field (mandatory) Taxcode, Indtype;


  validation ValidTaxCode on save { create; field Taxcode; }
  validation ValidIndTtpe on save { create; field Indtype; }

  draft action Edit;
  draft action Activate optimized;
  draft action Discard;
  draft action Resume with additional implementation;

  draft determine action Prepare
  {
    validation ValidTaxCode;
    validation ValidIndTtpe;
  }


  mapping for ztax_606
    {
      Uuid               = uuid;
      Taxcode            = taxcode;
      Indtype            = indtype;
      LocalCreatedBy     = local_created_by;
      LocalCreatedAt     = local_created_at;
      LocalLastChangedBy = local_last_changed_by;
      LocalLastChangedAt = local_last_changed_at;
      LastChangedAt      = last_changed_at;
    }
}