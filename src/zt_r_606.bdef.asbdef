managed implementation in class zbp_t_r_606 unique;
strict ( 2 );
with draft;

define behavior for ZT_R_606 alias map606
persistent table zmap_606
draft table zmap_606_d
lock master
total etag LastChangedAt
etag master LocalLastChangedAt

authorization master ( instance, global )

{
  create ( authorization : global );
  update;
  delete;

  field ( readonly, numbering : managed ) Uuid;
  field (mandatory) Saknr, Dgiitype, Gdsserv;

  validation validateSaknr on save { create; field Saknr; }

  draft action Edit;
  draft action Activate optimized;
  draft action Discard;
  draft action Resume with additional implementation;

  draft determine action Prepare
  {
    validation validateSaknr;
  }


  mapping for zmap_606
    {
      Uuid               = uuid;
      Saknr              = saknr;
      Dgiitype           = dgiitype;
      Gdsserv            = gdsserv;
      LocalCreatedBy     = local_created_by;
      LocalCreatedAt     = local_created_at;
      LocalLastChangedBy = local_last_changed_by;
      LocalLastChangedAt = local_last_changed_at;
      LastChangedAt      = last_changed_at;
    }
}