projection;
strict ( 2 );
use draft;

define behavior for ZT_C_DGII_TYPE alias dgiitype
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}